# TODO

## 客户端更新

### 热更新架构（暂缓）

- 引入固定 Launcher 与版本化运行目录。
- 支持后台下载、原子切换 `current` 版本及启动失败自动回滚。
- 区分可热替换的前端/业务资源与必须重启的 EXE、Python Runtime、DLL 和数据库迁移。
- 设计签名版本包、旧版本清理和跨版本兼容策略。

当前优先保证冷更新可靠：签名清单缓存、Gitee 分卷下载、流式重试、安全续传、GitHub 完整安装包兜底、静默安装及启动失败回滚。

## 问题工单

### 诊断信息增强（计划小版本实现）

- 创建工单时固化脚本版本和执行日志快照，避免原日志清理后证据丢失。
- 附带客户端、Agent 版本、Agent ID、连接状态和基础运行环境摘要。
- 增加轮转的 `agent.log` 与 `desktop.log`，上报时仅采集限定行数的日志尾部。
- 对密码、Token、代理凭据、签名 URL、环境变量和运行参数统一脱敏。
- 限制诊断数据大小与保留期限，并明确提示用户将要上报的内容。
- 在工单详情中展示诊断摘要、采集状态及必要的受控下载入口。
- 补充数据库迁移、权限边界、异常场景和脱敏测试。

优先启用现有 `Issue.script_version`、`Issue.log_snapshot` 等预留字段；按“基础诊断快照”和“应用自身日志”两期交付，避免一次引入大型诊断包系统。
