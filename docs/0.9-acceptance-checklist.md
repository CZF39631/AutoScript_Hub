# AutoScript Hub 0.9 验收证据清单

填写日期、提交 SHA、产物 SHA-256、执行人和证据位置。未执行、模拟结果或临时替代镜像不得标记通过。

## 0.9 简明交付门槛

以下五项全部通过即可接收 0.9；后文未勾选的兼容性扩展和跨版本实升项目不阻塞本次交付。

- [x] Windows 11 x86-64 安装包可安装、启动，且不依赖系统 Python、Node 或 Git。
- [ ] Linux ARM64 与 AMD64 Docker 镜像均可启动并通过健康检查。
- [x] 局域网用户可登录、上传脚本、执行脚本，Compose 重建后数据仍保留。
- [ ] GitHub/Gitee 至少一个公开 Release 可匿名下载全部 0.9 资产及已签名更新清单。
- [x] 仓库内 Skill 与独立 Skill ZIP 均可校验并规范化脚本。

## 本轮验收记录（2026-07-21）

- 当前代码尚未提交：基准 `ea5985fb50aa89d2b97f0308d3289e2b9bf2d990` 加工作树改动；创建发布标签前必须先形成唯一验收提交。
- ARM64 正式容器：`autoscript09formal-server-1`，Compose 项目 `autoscript09formal`，镜像 `ghcr.io/czf39631/autoscript-hub-server:0.9-formal`，镜像 ID `sha256:54e4441cbee2e02fdbb91ba08f415a894f05eac07950073a8f1938f57f70bf4f`。
- 正式验证地址：`http://192.168.1.106:18091/`；正式容器健康接口返回 `0.9.0`、`beta`、`ok`，浏览器登录后可见脚本管理页和 3 个脚本。
- 自动化：Python `166 passed`；前端 `5 passed`；`compileall`、`pip check`、ESLint、Vite 生产构建均通过。
- Windows 安装包：`AutoScript-Hub-Setup-0.9.0.exe`，31,083,579 字节，SHA-256 `1462b6014d4f299284869192921b62263b89ba4e2a3ee304714f73ab1555de7b`；使用仓库私有 Python 3.11.9 构建环境生成。
- 独立更新器：`AutoScriptUpdater.exe` 单文件大小 8,067,447 字节；复制到不含 `_internal` 的隔离目录运行 `--help`，退出码为 0。
- 更新清单：客户端内置 Ed25519 公钥验签通过，版本 `0.9.0`、通道 `beta`，安装包大小和 SHA-256 与清单一致；最终目录 5 个资产的 `SHA256SUMS.txt` 全部复算一致。
- Windows 11 23H2（构建 22631.6199）实装：当前用户静默安装返回 0；安装目录内 CPython 3.11.9 能创建带 pip 的 venv；PATH 仅保留 `C:\Windows` 和 `System32` 时，UI、Agent、本地页面和运行时诊断均通过；关闭 UI 后 Agent 仍在线。
- 当前源码 ARM64 候选镜像：`autoscript-hub:0.9.0-candidate-20260721t1906`，镜像 ID `sha256:c42d33ed46dee933010ef787d8d6203fca2f505dc00f2e07c1e815e8b98c4944`；独立 Compose 项目 `autoscript09candidate1906` 运行于 `http://192.168.1.106:18092/`，健康三项为 `ok`，未替换或并入其他业务编排。
- ARM64 当前源码持久化：创建账号、上传脚本、成功执行并生成日志后，删除容器并以相同镜像和数据目录重建，再切换到独立 Compose 项目重建；账号、脚本、运行记录、日志、数据库大小与脚本 SHA-256 均保持一致。
- 局域网页面实测：在真实浏览器中登录候选服务、保存执行参数、提交执行，并上传实际 Python 文件；页面显示上传成功和脚本数量增加，测试产生的等待任务随后已取消。
- 独立 Skill ZIP：解压后单文件模板严格校验为零错误、零警告；多文件模板经随包打包器生成 ZIP 后再次严格校验为零错误、零警告。
- 发布源现状：本地签名清单目前仅含尚未发布的 GitHub `v0.9.0` URL；GitHub Release 和匿名回读仍是发布阻塞项，Gitee 作为国内镜像可在仓库和 Token 配齐后追加。
- 后续明细用于 1.0 前扩展验收；未勾选项目不再自动阻塞 0.9，但不能据此声称对应平台或场景已验证。

## Linux 服务端

- [x] 真实 aarch64 主机从 0.9 部署包和正式镜像启动，就绪三项为 `ok`。
- [x] 同一局域网浏览器打开登录页、登录、上传脚本并执行。
- [x] Compose 重启和重建后账号、脚本、运行记录与日志保留。
- [x] 完成备份、数据变化、停止恢复，验证恢复结果。
- [x] 完成一次正式镜像升级和一次回滚。
- [ ] 同一镜像的 linux/amd64 启动与健康检查通过。

## Windows 客户端

- [ ] Windows 10 x86-64 安装、首次向导和启动通过。
- [x] Windows 11 x86-64 当前用户安装和已配置启动通过。
- [ ] Windows 11 x86-64 首次向导完整填写和重启通过。
- [x] 清空系统 Python/Node/Git PATH 后仍可运行。
- [x] 诊断显示私有 Python 3.11.9 的安装路径和版本。
- [x] 两组依赖产生不同指纹环境，相同依赖复用环境。
- [ ] 在线执行、日志、本地结果打开通过；结果文件未上传服务器。
- [ ] 断网冷启动、缓存脚本浏览、离线执行及恢复同步通过。
- [x] 关闭 UI 后独立 Agent 继续运行并响应本地状态接口。

## 更新与回滚

- [ ] 真实安装版完成 0.9.0 → 0.9.1。
- [ ] GitHub、Gitee、局域网地址提供同一已签名清单。
- [x] 首选源不可达时自动尝试下一下载镜像。
- [x] 坏签名、坏长度和坏 SHA-256 均被拒绝。
- [x] 未经用户确认不安装；脚本运行时进入 waiting-for-idle。
- [ ] 新版本启动失败时恢复上一安装器。
- [ ] 更新后配置、脚本、环境、日志、运行历史和结果仍存在。

## AI Skill

- [x] 单文件脚本生成、验证、上传和私有环境执行通过。
- [ ] 不合规旧脚本审查并修复到零错误、零警告。
- [x] 多文件 ZIP 根目录含 `main.py`，验证、上传和执行通过。
- [x] 独立 Skill ZIP 解压后使用随包工具严格校验单文件与多文件样例。
- [ ] 将独立 Skill ZIP 安装到全新 Codex 环境并验证自然语言触发。

## 自动化与发布资产

- [x] Python 全量测试、compileall、pip check 通过。
- [x] 前端测试、lint、build 通过。
- [ ] Windows 安装、更新、启动确认和回滚测试通过。
- [ ] Docker 双架构构建、启动、健康和持久化测试通过。
- [x] 所有约定资产存在，`SHA256SUMS.txt` 全部校验通过。
- [ ] GitHub/Gitee Release 匿名回读成功，Gitee 单文件小于 95 MiB。

## 晋级决定

- 验收提交 SHA：
- Windows 0.9.0 安装包 SHA-256：`1462b6014d4f299284869192921b62263b89ba4e2a3ee304714f73ab1555de7b`
- 服务端镜像 digest：本机 ARM64 镜像 ID `sha256:54e4441cbee2e02fdbb91ba08f415a894f05eac07950073a8f1938f57f70bf4f`；仓库多架构 manifest digest 待正式发布后填写
- 验收结论：通过 / 不通过
- 用户是否同意从同一提交创建 `v1.0.0`：
