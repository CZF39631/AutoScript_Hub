# v1.2.5 发布与正式部署记录

## 不可变发布

- 正式 Tag：`v1.2.5`，提交 `7824a94f17dc4a6d9facde105af6396652e794f3`。
- [主分支 CI](https://github.com/CZF39631/AutoScript_Hub/actions/runs/35836445966) 与 [发布流水线](https://github.com/CZF39631/AutoScript_Hub/actions/runs/35837142249) 均通过。
- GitHub/Gitee 正式 Release 已公开，非预发布；两端 Tag 一致，旧 v1.2.4 Tag 和资产未改动。
- GitHub 提供安装包及全部资产；Gitee 镜像部署包、Skill、签名清单、签名和校验文件，不镜像 EXE。
- Gitee 匿名下载及 Ed25519 签名验证通过；CI 完成 GitHub 匿名下载校验。维护电脑访问 GitHub Release 下载遇到网络异常，后通过同一发布流水线的 `windows-release` 原始产物完整下载 EXE，并与公开签名清单核对通过。
- 安装包：`AutoScript-Hub-Setup-1.2.5.exe`，34,664,333 字节。
- SHA-256：`9bf2b87f4e0cb0c544927f095e841c73825a33e910a344d08f0593a9ab3f5875`。
- `linux/amd64` 与 `linux/arm64` 镜像元数据及发布流水线就绪检查通过。

## 正式服务器升级

- 2026-09-23 09:26:13–09:26:36 UTC，从维护电脑使用标准运维脚本升级到 `1.2.5 stable`，退出码 0，未触发回滚。
- 先预拉镜像，旧服务继续运行；未更改 `SKIP_PULL=0`，配置备份与 SQLite 在线备份均保留。
- 以 0.5 秒间隔采样，内外网不可用窗口约 13 秒；不是无停机升级。
- 内外网恢复 ready，版本和渠道正确，`database`、`data_dir`、`migration` 均为 `ok`；容器 healthy、SQLite 完整性通过，迁移仍为 `0005_server_settings`。
- 升级前后核对运行任务：既有任务继续执行或正常完成，后续任务正常运行；恢复超过 90 秒后再次确认 Agent 心跳新鲜，未发现核对任务被看门狗异常置失败。没有停止客户端、取消任务或人为修改任务状态。
- 私有连接配置、旧镜像 ID、备份位置和任务明细仅保存在受忽略的维护证据目录，不写入此记录。

## 安装包交付

- 正式签名原包从维护电脑上传至服务器，传输前后 SHA-256 一致；这不是本机重新编译后替换同版本资产。
- 在远端私有配置中启用安全同步令牌，保留原配置、数据库和缓存备份；使用标准流程重建同一正式镜像。此轮观测不可用窗口约 13.75 秒，未触发回滚。
- 通过应用认证接口 `POST /api/release/sync` 上传原包，返回 201，与后台自动下载共享发布锁；没有从独立进程并发写缓存，也未关闭自动下载。
- stable/latest 缓存为 `1.2.5`；旧 `1.2.3`、`1.2.4` 缓存保留。签名清单未改写，客户端可优先从当前服务器安装包入口下载。
- 维护电脑通过正式公网入口匿名完整下载 34,664,333 字节，SHA-256 与上述正式资产一致；Ed25519 验签通过。
- 重建超过 142 秒后再次核对：服务 healthy/ready、数据库完整性正常，跟踪任务继续运行或自然成功，Agent 心跳新鲜。未安装或替换任何本机客户端。

## 维护约定

纯 UI 变化优先更新对应前端资源，不为了样式或交互反复发布客户端；涉及 Agent、原生接口、运行时或兼容协议的变化才评估客户端升级。运行任务不是兼容服务端升级的硬性阻断，但必须控制心跳断连窗口并检查恢复情况。
