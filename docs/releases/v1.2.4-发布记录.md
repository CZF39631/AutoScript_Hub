# 1.2.4 发布记录

## 冻结版本

- 用户已验收 `1.2.4-review.2` 并授权正式上线。
- 正式 Tag：`v1.2.4`，源码提交：`461c7c1519bfc6946a171f7f1fce689330dfaed5`。
- 最终主线 CI：`34448791489`；Gitee API 冒烟：`34448816083`；Tag CI：`34449317839`，均通过。

## 首次发布中断

Release `34449318112` 的 Windows 构建因测试环境缺少 PyYAML，在 pytest 收集阶段失败；两站 Release、签名清单及安装包未发布，服务端未升级。

部署包、Skill 与双架构镜像构建和运行冒烟均成功。两个架构就绪响应均为 `1.2.4 stable`，数据库、数据目录和迁移检查通过。

已公开镜像摘要必须保留，不因安装包构建失败而重推：

- Index：`sha256:999b7a2068dca823dc5bcaba04d84c0b55045065d5655eb0a0f79ecbba81a069`
- amd64：`sha256:7ef5c58f41aecab09f89cda03f360cdb9278ad41aa27b1181b04eab5648a92bb`
- arm64：`sha256:048596de9e8ee11185f788ca31a77345d682876df9a2f4d65890e0e72d9ac1c1`

## 恢复方案

- 后续版本的 `build.ps1` 显式安装 `PyYAML==6.0.2`，与测试依赖匹配。
- 一次性工作流 `.github/workflows/release-1.2.4-recovery.yml` 从冻结提交构建客户端，仅在构建环境补装缺失依赖，不改源码或 Tag。
- 复用首次运行的 `portable-bundles`，校验已公开镜像摘要，不重建镜像。
- 在独立 Windows Runner 安装并冒烟验证正式客户端，再沿用既有签名及两站发布步骤。
- 恢复前必须确认两站不存在该版本 Release；公开后失败保留资产，禁止自动删除、覆盖或移动 Tag。恢复工作流不能用于重复发布已有版本。

## 服务端验收边界

升级前只读检查确认当前服务为 `1.2.3 stable`，容器 healthy，内网就绪接口和 SQLite 完整性通过，迁移为 `0005_server_settings`。本机维护配置未明确提供公网健康入口，不能将内网检查描述为公网检查通过。

## 正式发布结果

- 恢复工作流 `34451197043` 成功；运行入口提交为 `447ef640b0bdfbbc239f8a5d80b747de713745a2`，实际产物源码仍为上述冻结 Tag。
- 恢复流程 CI 415 项、冻结源码构建测试 413 项通过；Windows 构建 Runner 上的安装冒烟通过，确认 Agent `1.2.4`、托管 Python `3.11.9` 及桌面 UI 可用。该验证不等同于在无构建依赖的新 Runner 上测试。
- GitHub Release ID `386093189`，非 draft、非 prerelease，六项发布资产齐全。
- Gitee Release ID `1135285`，非 prerelease，五项镜像资产齐全；另有平台自动提供的源码 ZIP/TAR.GZ，不属于上传的发布资产。
- 两站 Tag 均指向冻结提交。首次发布的镜像 Index 和两个架构摘要未改变。
- Runner 的 GitHub 标准下载地址匿名校验通过。本机直连 GitHub 网页下载域名发生 TLS 中断，改用官方资产 API 匿名下载，未关闭 TLS 校验；Gitee 五项资产从标准下载地址匿名取得，与 GitHub 同名文件逐字节一致。
- 本机独立验证两站 Ed25519 清单、版本/渠道、全部 SHA-256，以及安装包长度；GHCR 匿名核验双架构索引、配置及源码标签。
- 正式安装包：`AutoScript-Hub-Setup-1.2.4.exe`，34,592,445 字节。
- 安装包 SHA-256：`eba408632a409aab4cab1d675f54cd1cc9b56f35a9a13c0c56d95811ad5a5ed7`。

## 服务端升级结果

- 已通过维护脚本升级至 `1.2.4 stable`，退出码 0，无回滚触发。
- 线上架构 `arm64`，容器 running/healthy，镜像源码标签和 RepoDigest 与已发布版本一致。
- 服务端本机与已配置内网入口均 ready；database、data_dir、migration 均 ok。
- SQLite `integrity_check=ok`，迁移为 `0005_server_settings`。
- 已核验本次升级前的 `1.2.3` 数据备份清单、长度和哈希，环境配置备份保留。
- **未完成：公网健康入口未明确配置，尚未执行公网入口验收。** 未在生产运行测试业务脚本，也未改动维护电脑的已安装客户端。

发布页：

- https://github.com/CZF39631/AutoScript_Hub/releases/tag/v1.2.4
- https://gitee.com/chuzifeng/auto-script_-hub/releases/tag/v1.2.4
