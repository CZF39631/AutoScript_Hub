# 1.3.0 Preview 2 体验说明

## 定位

- 版本：`1.3.0-preview.2`，独立 Preview 安装家族；构建渠道字段仍为 `beta`，不代表与正式安装共用数据。
- 源码分支：`preview/1.3.0-preview.2`。
- 正式版本仍为 `v1.2.4`；本次不合并 main、不创建正式 Tag 或 GitHub Release，不自动升级服务端。

## 相比 Preview 1

- 登录页和侧栏支持简体中文 / English 切换，覆盖 React 主界面的固定文字和日期控件。
- 语言切换保留设置和脚本表单，不修改 API、脚本参数 key、执行状态、权限或时区。
- 改善英文按钮、矮窗口侧栏和固定表格列的显示。
- 新增 English README，并整理中文说明、下载入口和安全边界。

脚本自带文字、原始日志、历史更新正文、原生向导和桌面通知不在本轮翻译范围。参见[多语言支持](../多语言支持.md)。

## 安装与使用

体验包名称：`AutoScript-Hub-Preview-Setup-1.3.0-preview.2.exe`。已完成本地构建和冻结验收，未上传公开 Release；不要把分支预览链接当作安装包下载地址。

- 手动运行独立 Preview 安装包；默认使用 `%LOCALAPPDATA%/Programs/AutoScript Hub Preview` 和 `%LOCALAPPDATA%/AutoScriptHubPreview`。
- 同一 Preview 家族沿用原 Preview 数据，安装前建议备份；不要选择正式版目录或正式数据根。
- 原生向导仍为中文；进入登录页后即可切换 English。
- 不主动读取或迁移正式版凭据。Preview 暂停在线更新，不应通过 Stable / Beta 更新入口安装本包。
- 本次不自动替换现有测试服务；连接测试服务时使用独立测试账号。
- 脚本不是沙盒，仍使用当前 Windows 用户权限。使用可信测试脚本，勿将测试参数指向正式业务文件。

更多边界见 [Preview 安装与数据隔离](../Preview安装与数据隔离.md)。

## 构建与验收记录

- 安装包源码：[335b823804a2610a604b22e8059474c23f55500e](https://github.com/CZF39631/AutoScript_Hub/commit/335b823804a2610a604b22e8059474c23f55500e)；从 Git archive 导出，370 个源码文件在构建后逐一复核哈希一致。
- 安装包大小：**33,756,034 字节**。
- SHA-256：`45403a3bac0b8e7a1012457397174fa938889d5e314da1c3fa7bfd3410599a03`。
- 安装包、源码清单、校验文件、冻结和界面验收记录随本地体验包交付；未提交 EXE 到 Git。
- 构建时 **714 项 Python、59 项前端测试通过**，lint/build 成功，生产 npm 依赖审计为 **0 漏洞**。
- [构建提交 CI](https://github.com/CZF39631/AutoScript_Hub/actions/runs/34930525541) 的 Windows/Linux Python、前端、amd64 Docker 冒烟及依赖审计全部通过。
- 后续提交 `c4e6dc6` 仅将冻结验收脚本从固定 Preview 1 改为明确指定 Preview 版本；相关 **26 项测试通过**，未改变本包运行时代码。
- 冻结 Agent 确认为 `1.3.0-preview.2` / `preview`；独立默认数据、端口家族、令牌和 Origin 隔离通过。
- Preview 关闭后合成正式版 API 仍工作，合成正式版全部文件哈希不变；在线更新 API 与独立更新器均拒绝更新，伪造 Stable 环境变量不能解除冻结身份限制。
- 冻结执行验证：已缓存无依赖脚本成功；离线缺少依赖时拒绝执行。验收进程清理确认完成。
- 冻结目录中的前端资源与本次构建产物逐字节一致；使用这些资源进行浏览器合成接口验收，中英文切换、表单保留、时区和原始内容保留、全部主路由通过，确认显示 Preview 2 更新标题。1440/800 宽度无页面横向溢出，零页面错误。

### 限制与警告

- **未执行 Setup，未验证真实安装或双机。** 并存对端是冻结 1.2.4 API 的合成进程，不是实际安装的旧客户端；浏览器接口也是合成数据。
- 未替换现有 Preview 1 测试服务，未改动生产、已安装客户端或旧安装包。
- npm 完整依赖报告仍有 1 moderate / 1 high 开发依赖告警；生产依赖审计为零。另有 Python 弃用、前端 chunk 大小、PyInstaller 可选隐藏导入及 Inno 未使用变量等非阻塞警告。
- 构建期间 npm 空配置冲突和语言文件下载失败已修复后完整重跑；语言文件复用既有缓存，且 SHA-256 与构建脚本固定值一致，没有跳过测试。
