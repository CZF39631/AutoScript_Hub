# 1.3.0 诊断恢复进度

## 阶段一：盘点（已完成）
- 保留 feature/diagnostics-scheduling 的现有半成品；不切分支、不提交、不触碰 tmp 或私有配置。
- 已读指定服务端、客户端与前端文件。现有策略、白名单 collector、快照墓碑和下载接口具备基础实现。
- 待补：两阶段显式同意表单、网页无 Agent 报单入口、版本与采集状态展示、隔离回归测试；加固快照 consent 与字节边界。
- UI 保留现有 AntD；技能上下文已读取，不重设计。
- 协作边界：scheduler 清理由父 agent 接入；0007 由服务端 agent 处理；/local/diagnostics 与可信 effective 同步由客户端 agent 接入。

## 阶段二：闭环实现（已完成，测试进行中）
- 新增 DiagnosticReport 与纯函数 diagnosticReport：默认不采集，手动本机白名单采集、逐项选日志、明确服务器预览提示、二次确认创建；取消使异步预览失效。
- Issues 增通用报单、固定版本/状态展示、认证下载；RunDetail 改为相同表单，去除“日志自动附带”误导。
- 服务端加入 strict consent 和 include_run_summary；未选择摘要不保存 Run 参数/错误，版本始终固定。预览和创建分别重验权限/同意/字节界限。
- 0008 使用冻结显式 schema，不导入运行期模型。
- collector 落盘内容有界并拒绝符号链接；测试发现标准 RotatingFileHandler 对中文按字符估算导致超字节限，已改按实际 UTF-8/Windows 换行字节轮转。
- 初次隔离测试 10 通过、1 失败（上述轮转问题）；修复后扩展回归已全部通过。测试使用内存 SQLite、独立 FastAPI（无 lifespan）、临时伪 paths，不启动真实 Agent。

## 阶段三：加固与隔离验收（已完成）
- 请求流在 JSON 解析前限制为 192 KiB；创建、预览、下载再次验证 UTF-8 字节上限；工单响应加 no-store。
- 摘要与执行日志分开显式选择；即使未选择参数，仍使用 Run 已知敏感值清理所选应用日志。后端日志的截断提示计入 500 行预算。
- 移除自定义字段（含总开关已关闭时）仍需确认风险，字段名大小写变更不算移除。设置页说明保留期限仅影响新快照，落盘基础脱敏不可逆且不能保证识别所有敏感内容。
- 验证命令与结果：
  - `python -m pytest -q test/diagnostics/test_diagnostic_recovery.py`：18 passed（含 strict consent、请求流超限、管理员/工单权限、独立开关、固定版本、过期不回退、清除、0008 独立迁移、白名单/符号链接/中文轮转）。仅有 Starlette/httpx 弃用警告。
  - `node --test frontend/src/utils/diagnosticReport.test.js`：4 passed（网页无 Agent、逐项日志选择、UTF-8 上限、取消迟到预览、冻结请求及只提交一次）。
  - 定向 ESLint（DiagnosticReport、DiagnosticSettings、Issues、RunDetail、diagnosticReport 工具及测试）：通过。
  - 定向 Python compileall 和 tracked 文件 diff --check：通过；Git 仅提示已有 LF/CRLF 自动转换。
  - Impeccable 对四个改动 UI 文件的机械扫描：空 findings。
- API 协作契约：IssueCreate 新增 `include_run_summary: bool`（默认 false，严格布尔）；选择执行参数/错误摘要时必须同时传 `diagnostics_consent: true`。仅传 consent 不会隐式附带摘要。`include_run_log` 单独控制执行日志；`diagnostics.application_logs` 仅允许 agent/desktop。
- 本机接口保持既有 `GET /local/diagnostics` 平面响应；collector 三函数签名未变。已只读核对客户端 agent 使用相同响应契约。

## 限制与交接
- 未启动真实 Agent、未访问生产、未读写 tmp/私有 env，未切分支、提交、推送或更改 App/任务 UI/main/scheduler/models。
- 未跑全量测试、未启动浏览器做实际渲染/交互验收；前端取消测试是纯工作流单测，不能替代浏览器端到端测试。完整 0007→0008 链由集成方验证，本阶段只隔离验证 0008。
- 服务器预览会实际发送用户所选内容，但不持久创建工单；取消不撤回已发出的请求。提交时按最新策略/权限重新处理，因此预览之后的策略变化可能改变最终脱敏结果。
- 保留工单标题、描述和解决说明属于业务工单记录；自动过期清除的是诊断 payload 与附带执行日志。已有历史工单仍沿用历史兼容读取，仅新快照过期墓碑明确禁止回退 Run。
- 当前分支其他 agent 同时修改的文件仍保持原状；请父 agent 集成审查新增 DiagnosticReport/diagnosticReport 与测试文件。

## 阶段四：集成反馈修复与浏览器验证（已完成）
- 旧 `backend/tests/test_issue_diagnostics.py` 的所选快照 helper 显式传 include_run_summary/include_run_log/diagnostics_consent；管理员超限用例也显式选择日志。未降低旧断言，默认不采集的新测试保留。
- cleanup 每次按 expires_at、issue_id 排序限 500 条；新增 1000 条到期记录两批续清及未来记录保留测试。只读核对 scheduler 已每 30 秒调用并 commit，未修改父 agent 的 scheduler。
- 新增独立 safeError 工具及异常形态测试，DiagnosticReport/DiagnosticSettings 不再将 Pydantic 数组或对象直接传给 React；不显示 error.input。未修改 taskScheduling.js。
- DiagnosticReport 使用 AntD 6 `mask={{ closable: false }}`，移除弃用参数。
- 新旧诊断 Python 合跑：29 passed（19 新 + 10 旧），3 项既有依赖/生命周期弃用警告；前端诊断/safeError 单测 5 passed，定向 ESLint 通过。
- 使用独立 `diagnostics-130` 浏览器 session、现有 Vite 5327，所有 API 与 Agent 请求均拦截 mock。验证无 Agent 报单、预览取消不创建、422 数组安全显示、本机采集不上传、未同意不请求、只上传所选 Agent 日志及确认创建；pageerrors=0。mock 的失败请求会有网络控制台错误，不是 React 崩溃。
- 浏览器脚本 `frontend/tests/diagnostics.browser.js` 可复用。初次脚本因沙箱无 URL 全局和 AntD「取 消」按钮定位失败，修复测试脚本后完整通过。已关闭该独立 session，未触碰 tasks-130。

### Tasks/App 只读 finish review
- 已读取 Tasks.jsx、App.jsx 及既有 tasks-1440.png/tasks-800.png。视觉继承原应用主题、侧栏、表格和按钮；固定版本与本机授权确认文案明确，没有重设计建议。
- P2 功能边界：Tasks.load 的不可用分支提前 return，未清 loading；若加载中断网/Agent 断开，旧请求因 serial 失效也不会清 loading，表格和刷新可能一直转圈，直到连接恢复。建议父 agent 在不可用分支 setLoading(false)。
- P2 可操作性：远程任务表只有脚本/版本，没有目标设备名或编号；多设备相同名称任务无法从列表辨别实际执行电脑，运行一次/删除时也未补目标身份。建议远程任务列/确认框明确 device。
- P3 布局：800px 截图中表格的操作列完全在右侧视口外，且当前截图看不到明显横向滚动提示；页面本身没有横向溢出。可考虑操作列固定右侧或提示横向滚动，无需改变现风格。
- 以上只报告，未修改 Tasks.jsx/App.jsx 或任务测试；截图只读取，未覆盖。
