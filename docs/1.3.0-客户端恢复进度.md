# 1.3.0 客户端恢复进度

## 阶段一：接手检查
- 已完整读取调度契约、四个半成品 runtime 文件及 Agent 两个实际入口。
- 已检查工作区，保留全部已有改动；不切分支、不提交、不触碰根 tmp 与真实客户端。
- 当前缺口：runtime 未接入口；旧执行仍直接 Popen/kill，离线历史仍 execute 创建 pending。
- 下一步：先补进程树停止确认与账本隔离测试，再接统一执行和本地 API。
- 初次测试：9 项隔离 runtime 测试，6 通过、3 失败，发现既有 events INSERT 占位符 10 个而表仅 9 列；已修复待复测。

## 阶段二：执行入口与接口已接线（待验证）
- 新统一异步控制器：本机、旧线上、新远程包含 prepare 占槽；意图先落 SQLite；monotonic 截止与取消；start_intent 先于 start 请求，ACK 丢失不执行。
- Windows 命名 Job 支持重启重新打开并确认停止；正常结束也清理后代，无法确认停止保留槽。
- 实际 Agent 已接本地任务、远程 claim/start/report、纯离线 tick、固定缓存版本及参数校验、完成历史 import-local、诊断策略同步与日志。
- 本地 API 已接 schedules/events/device/grants/cancel/diagnostics，并沿用 Bearer+Origin。
- 保留旧入口辅助代码但改名为 legacy，实际主循环不再调用；后续需清理死代码并回归旧 Agent 测试。
- 已补入口隔离测试与 Windows Job 模拟测试，并复测。

## 阶段三：已验证及交接边界
- `python -m pytest -q client/tests/test_task_runtime.py client/tests/test_task_agent.py`：20 passed（最后一次 1.23 秒）。
- 六个目标 Python 文件 `compileall` 通过；两个已跟踪入口文件 `git diff --check` 通过。
- 测试使用临时 ClientPaths、SQLite、mock 网络/进程；未启动真实客户脚本、Agent 服务或生产连接。
- 修复原半成品 events INSERT 列数错误；UTC 存储、最近发生项、暂停/删除 queued 取消、恢复刷新 next_fire、手动 UUID 幂等已有测试。
- 确认正常父进程退出仍先清理后代再报告；停止不确定不释放槽；prepare 占槽期间主循环 heartbeat 可继续。
- 远程 outbox 与本地历史每轮仅发一条；失败记录轮转，避免积压或失效记录长期阻塞 heartbeat/其他报告。
- 固定版本参数验证、start ACK 丢失仅报告 unknown、不重复 claim/start、JWT+设备头终态 import-local、API Origin/Bearer 及诊断失败默认已有测试。
- 仍待父 Agent 后续验收：旧 Agent 实现细节测试迁移（未运行旧测试）、实际 Windows Job 集成测试（此次仅模拟，未启动脚本）、UI 全链路联调。
- 维护债务：main.py 内旧直接执行辅助实现已改名并从实际入口断开，但死代码尚未删除。不得重新接回 `_legacy_*`、`_start_local_run_locked`、`_poll_and_execute_locked`。
- 兼容边界：旧环境仅保留浏览器/输出目录/代理显式应用字段，不接受 `extra_env` 或远程 `python_executable`。本机执行拒绝任意环境覆盖。
- 本阶段分支未切换；未暂存、提交、合并或推送。

## 阶段四：父 Agent 审查后的完整修复与全量回归（最新结论）

### 已完成
- 公开 `start_local_run` 保持错误字典兼容；忙碌先拒绝、授权先于缓存版本查找。
- `_initialize_tasks` 加初始化锁，失败关闭 SQLite/门禁，支持安全重试；并发 HTTP 调用只创建一个控制器。
- schedule-events 提供 flat script_id/script_version/task_name/state；只有真实本地记录才返回 local_run_id，远程及旧线上使用 run_id。
- 设备授权列表离线返回缓存或空数组，公开身份附 grants_online；离线决策不使用缓存授权，而是失败拒绝。
- unknown 状态及重启恢复暂停关联本机任务，取消其他 queued；pause/delete 对准备和运行中的任务发取消请求，queued 立即 cancelled。
- paused 任务拒绝 run；领取前重检周期/一次触发宽限，手动发生项采用 300 秒 TTL；过期记录 skipped，不执行。
- 完成历史首次冻结完整 import_payload 并持久化，重试不重读日志改变请求内容；已存在 backend_run_id 的旧历史仅核对同一记录，不创建另一个 Run，不调用 execute。
- 旧历史归属、脚本与版本匹配且处于 running 时允许补其终态；日志上传失败不阻止终态补报，但保持待日志重试。已终态的服务端记录绝不覆盖；仍 pending 的旧导入保持人工核对，不领取执行。
- 结果标记畸形、日志读取/上传异常不丢终态；旧线上完成报告也写入 SQLite outbox。终态或 outbox 持久化失败保留 active 槽。
- 已删除旧直接执行、准备、单父进程 kill、execute 导入路径等死代码；不再依赖断开入口保安全。
- 已迁移旧 Agent 回归测试到新控制器，保留版本锁定、抢占先于下载、双入口准备门禁、结果通知、唯一参数文件、历史幂等与权限隔离断言。
- 源码 Agent 入口已加 freeze_support；冻结发布入口由父 Agent 独占补早期 freeze_support，本子任务未修改该文件。

### 实际验证
- 全量 `client/tests`：**180 passed / 40.75 秒**。
- 输出：`C:/Users/39631/AppData/Local/Temp/ash-client-final-1th7idax/result.log`（UTF-8）。
- AUTOSCRIPT_CLIENT_DATA_DIR、DATA_DIR、DATABASE_URL 全部指向上述 OS 临时目录；未读实际客户端配置或使用默认 audit 数据库。
- 三项真实 Windows Job 集成测试通过：脚本首条用户代码已在 Job 内、父进程退出后清理仍存活后代、合成 Agent os._exit 后 kill-on-close 生效。所有进程均为临时目录合成 sleep 程序，只按本次创建的句柄/Job清理，未运行业务脚本。
- 初次真实 Job 测试 1 失败是测试本身未声明 64 位 Windows HANDLE 原型；补完整 ctypes 原型后全通过。
- 六个实现文件与五个任务/Agent测试文件 compileall 通过；已跟踪修改 git diff --check 通过（仅 Git 的 LF/CRLF 提示）。

### 剩余验收边界
- 未构建签名冻结安装包，尚需父 Agent 在最终打包产物验证 prepare 子进程的 freeze_support 分支；源码进程树已实际验证。
- UI 全链路、服务端及发布验证由父 Agent 负责；不宣称已发布。
- 旧 pending 历史导入、真正 unknown 的业务结果仍需人工核对，绝不自动重跑。
- 无提交、合并、推送或分支切换；未修改根 tmp、私有 env、生产或其他任务独占文件。
