# 1.3.0 服务端恢复进度

## 阶段一：实现补齐（待测试）
- 已完整读取接口契约、models/schemas/task_service/tasks 及旧 runs/agents。
- 保留当前分支与其他模块未提交改动，不操作 tmp、私有配置、生产。
- 新增旧 runs 接口受管/导入历史保护与 JWT + 设备 Token 的 import-local。
- claim 增加排队有效期；start 复核槽、授权、取消与领取有效期；queued 取消确认 stopped；已停止 unknown 不再改写。
- 调度使用 shared.latest_fire，删除 9 天循环；输出按 UTF-8 字节限制。
- 新增 0007，兼容现 metadata 已有表及老库创建表/补列/唯一索引。
- 父 agent 集成 main/scheduler/migrations；本阶段未修改这些文件或 agents.py（旧 Agent 路由不接触任务设备凭据）。

## 阶段二：安全复核与测试完成
- pending 在双用户访问权撤销后取消 queued、请求已领取执行停止，且不再返回参数。
- claim 授权失败会暂停任务并取消执行；start 用刷新后的状态和领取时间 CAS，取消胜出时不会返回 can_start=true。
- report 允许撤权后的设备上报停止事实，但不重新授予启动权；已确认终态不能被晚到不同终态覆盖。
- 服务端落盘输出同样限制 UTF-8 字节数；import-local 的并发唯一键冲突有限处理，不隐藏其他 IntegrityError。
- 已运行且仅运行本任务测试：`python -m pytest -q backend/tests/test_task_scheduling.py backend/tests/test_task_migration.py`。
- 最近结果：26 passed，3 个现有 FastAPI/Starlette 弃用警告；无失败。`git diff --check`（本任务路径）通过。
- 覆盖设备所有权/秘密、管理员不能代替设备授权、双用户各阶段访问权、固定旧版本、撤销、过期、unknown 不重跑、旧路由保护、终态导入、防路径注入、UTF-8 上限、多周积压、线程并发 claim/start/manual/import、取消与旧 Session 竞争、新库/老库/半成品迁移及重复升级。
- 增补 DST 周任务回归：2026-03-14 纽约周日 02:30 规则，跳过 03-08 春季缺失时刻，正确选取 13 天前 03-01 而非多周积压中的旧发生项，直接覆盖旧 9 天窗口缺陷。
- 尚未完成（父 agent 范围）：主应用注册、后台调度集成、完整迁移 head 和全套回归、最终审查提交。未提交、合并或推送。
